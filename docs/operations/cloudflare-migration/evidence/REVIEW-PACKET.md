# Cloudflare standalone migration: review packet

For Codex's independent review and deployment strategy (VERIFY-05). This packet is written from the committed code and the recorded runs. It does not claim live Cloudflare, provider or production behavior: nothing here was deployed, provisioned, or run against a real account, provider or production data.

## 1. Status

- Branch `feat/cloudflare-standalone`, base `35f90c7` (main, specification package), head `2e72fe3` (last code commit, after the Codex review round in section 12; this packet revision is committed on top of it). Worktree: `/Users/xulelin/code/openinterviewer-cloudflare`. Not pushed.
- Local release candidate: complete through M6 on this branch. The full local release matrix (`npm run check:cloudflare`, 18 lanes) passed on `2e72fe3`, a clean checkout (and earlier on `d8bb26e`; Codex reran it independently on `7235c80`); its receipt binds that commit and artifact. The packet itself is added in the following docs-only commit, which changes nothing else; rerunning the check on the branch head re-binds the receipt.
- Requirement map (appendix A): of 55 requirement IDs, met locally 40, implemented with acceptance at a remote gate 11, recorded deviations 4, partial 0.
- Remote-only gates (section 9) and operational decisions (section 10) remain, as VERIFY-05 allows. Production migration and the deploy button remain incomplete by design.

## 2. How to reproduce

From a clean checkout of the head commit with Node 24.19+ and npm (the repository's `package-lock.json` is authoritative; the workstation default of bun does not apply here), a local `redis-server` and Playwright Chromium:

```bash
npm ci
npm run check:cloudflare        # builds the artifact, runs every lane below, writes dist/cloudflare/artifact/receipt.json
```

Individual lanes: `npm run check` (lint, typecheck, unit), `npm run test:setup`, `npm run test:setup:cloudflare`, `npm run test:cloudflare` (workerd), `node scripts/cloudflare/check-import-boundary.mjs`, `npm run test:contract:redis`, `npm run test:redis-crash`, `npm run test:adversarial`, `npm run test:inventory:redis`, three Node builds, `npm run test:e2e`, `npm run build:cloudflare`, `npm run test:cloudflare:artifact`, `npm run test:cloudflare:restart`, `npm run test:e2e:cloudflare`. `npm run preview:cloudflare` runs the built artifact locally with synthetic providers. The release check removes credential-like environment variables from every lane and prints only their names.

## 3. Final release matrix

| Lane | Command | Result | Duration |
| --- | --- | --- | --- |
| sync-artifacts+lint+typecheck+unit | `npm run check` | 2735 passed (2735) | 22.2 s |
| setup-checker | `npm run test:setup` | 44 passed, 0 failed | 0.9 s |
| installer | `npm run test:setup:cloudflare` | 202 passed, 0 failed | 31.0 s |
| audit-production | `npm audit --omit=dev --audit-level=high` | 0 vulnerabilities (production dependencies, high and above) | 0.7 s |
| diff-check | `git diff --check` | exit 0 | 0.0 s |
| workers-runtime | `npm run test:cloudflare` | 390 passed (390) | 9.8 s |
| worker-import-boundary | `node scripts/cloudflare/check-import-boundary.mjs` | Worker-only graph reaches no Next, Redis or hosted modules | 0.2 s |
| redis-contract | `npm run test:contract:redis` | 26 passed, 2 skipped (28) | 1.5 s |
| redis-crash | `npm run test:redis-crash` | 34 passed (34) | 0.9 s |
| adversarial | `npm run test:adversarial` | 24 passed (24) | 0.9 s |
| redis-inventory | `npm run test:inventory:redis` | 14 passed (14) | 3.0 s |
| build:node-standalone | `npm run build` | build succeeded | 5.2 s |
| build:node-gateway | `npm run build` | build succeeded | 4.3 s |
| build:node-hosted | `npm run build` | build succeeded | 4.3 s |
| node-browser | `npm run test:e2e` | 5 passed (9.8s) | 10.3 s |
| cloudflare-artifact | `npm run test:cloudflare:artifact` | 35 passed (35) | 19.5 s |
| cloudflare-restart | `npm run test:cloudflare:restart` | 4 passed (4) | 188.6 s |
| cloudflare-browser | `npm run test:e2e:cloudflare` | 9 passed (51.2s) | 51.7 s |

Receipt: `status: passed`, commit `2e72fe36fa6d257157525dee797c1ce35ae56c08`, worker bundle SHA-256 `8685051e6ac42d4cb5f6f9b763b10d6096ffcec349c1565339b32dcd5ad1b284`, checked on 2026-09-24. Artifact upload 27534.37 KiB, gzip 5464.85 KiB. The other lanes are unchanged from `d8bb26e`, except for small timing differences. The previous run, on `d8bb26e`, had unit 2724, installer 181, workers 371 and upload 27413.90 KiB. Credential-like variables removed from the lanes (names only): CLAUDE_CODE_MESSAGING_TOKEN, MINERU_API_KEY.

## 4. Branch and scoped diff

47 commits, `35f90c7..d8bb26e`, then the packet (`7235c80`) and four Codex-round fixes (section 12):

- `d24c502` build: pin Cloudflare toolchain and add M0 runtime feasibility probe
- `8ee2629` build: typecheck Worker sources separately and ignore Cloudflare outputs
- `7162798` build: adopt @cloudflare/vitest-plugin and add runtime/storage contracts
- `a3d45df` feat(cloudflare): add WorkspaceStore Durable Object skeleton and Workers test harness
- `a526457` feat(cloudflare): add interview projection, durable port extension and consumer seam
- `24cdeb9` feat(cloudflare): real Worker entry, release tooling and artifact harness
- `0560952` build: add build:cloudflare and artifact test scripts
- `e56f7a3` refactor: move providerErrorResponse to a route-only module
- `29267b3` test(cloudflare): add Worker-only import boundary check
- `e471098` build: add Cloudflare setup/deploy script entries and ignore installation state
- `7d5e954` test(cloudflare): add production-artifact browser lane and readiness gate
- `a37b805` build(cloudflare): add combined local release check writing the deploy receipt
- `eb11566` test(cloudflare): normalize harness listen URL type
- `1b9c49b` fix(cloudflare): make the JSONC parser string-safe and strict
- `2b64c22` feat(cloudflare): type claimed analysis inputs and share the synthesis ceiling
- `4464a7d` feat(storage): Redis WorkspaceStore adapter, shared contract scenarios, context integration
- `07946fc` feat(cloudflare): capability-aware configuration, readiness and Redis fence
- `a86adb0` ci: add Cloudflare lanes and a gated production promotion job
- `e334d6f` feat(ui): durable analysis states, bounded polling and batch semantics
- `c7597e2` feat(cloudflare): WorkspaceStore domain transactions and durable store client
- `552d575` feat(cloudflare): streaming export, maintenance modes, operational backup and epoch activation
- `5e1ab91` refactor(cloudflare): bind the import RPC to the complete backup manifest type
- `6a350f2` feat(cloudflare): durable analysis generations, single-alarm scheduler and Queue consumer
- `d63066c` feat(cloudflare): checked-in installer with plan/apply/resume/verify/update
- `83dbfe5` feat(cloudflare): operator routes and CLI, atomic sign-in budget
- `2bc77a6` fix(cloudflare): honor response backpressure in the OpenNext wrapper
- `33a8d66` feat(cloudflare): integrate every standalone route with the WorkspaceStorePort
- `04c19af` feat(cloudflare): reader-compatible schema migrations and held-alarm wake-ups
- `fcac673` feat(cloudflare): preview:cloudflare runs the built artifact locally
- `c1d9aff` fix(cloudflare): keep node:stream external when OpenNext compiles its config
- `169c6be` test(cloudflare): browser journeys pass on the built artifact, plus recovery
- `d5649b7` test(cloudflare): per-provider API journeys through the production artifact
- `33ec0c6` feat(cloudflare): close store replies, bound bodies and lists, one hold policy
- `c60c7df` docs(cloudflare): operator runbook, agent skill, deviations register, guides
- `5e9a890` test(cloudflare): production-object restart lane and fault manifest (VERIFY-01)
- `80ec88d` perf(cloudflare): load only the frozen provider adapter, before the start marker
- `e42be63` docs(cloudflare): measurements and evidence index for the review packet
- `06807c1` fix(release): harden the CI promotion, deploy gate and release check
- `7e7465a` feat(ops): metadata-only inventory of the old Upstash storage (OPS-04)
- `3ed14f0` fix(cloudflare): re-arm under identity mismatch; minReaderVersion on real migrations
- `3aa83d4` feat(ops): guarded point-in-time restore entry point (OPS-03)
- `ae5d307` fix(cloudflare): one admin-password maximum everywhere it is accepted
- `8c926b3` fix(ui): a failed study or list read never empties a loaded register
- `2012b2a` test(cloudflare): admission identity through the artifact; internal-header stripping
- `6ff715f` docs(cloudflare): production transition runbooks and review corrections
- `c937470` fix(ui): a start refused by a workspace hold is a certain refusal
- `d8bb26e` test(cloudflare): batch/detail/automatic races, keyboard use and the keyless demo in the browser

Scoped diff `35f90c7..d8bb26e`: 313 files changed, 81357 insertions(+), 2778 deletions(-); through `2e72fe3`: 317 files changed, 82950 insertions(+), 2787 deletions(-) (the Codex round: 32 files, +1511/−191, of which +986 tests). Excluding the generated `cloudflare/worker-configuration.d.ts` (15,767 lines) and `package-lock.json`, about 59,240 lines, of which 32,658 are tests. By area: `src/` +9,577; `cloudflare/` +22,941; `scripts/` +7,614; `tests/` +32,658; `docs/` +1,683; `.github/` +127.

| Area | Main paths |
| --- | --- |
| Runtime contracts and capability resolution | `src/lib/runtime/`, `src/lib/hostedConfig.ts`, `scripts/check-setup.mjs` |
| Portable store port, Redis adapter, durable client | `src/lib/storage/` |
| Worker entry, OpenNext wrapper | `cloudflare/worker.ts`, `cloudflare/internalHeaders.ts`, `cloudflare/opennext/backpressureWrapper.ts`, `open-next.config.ts` |
| WorkspaceStore Durable Object | `cloudflare/workspace/` (schema, migrate, studies, participants, completion, reads, analysis, scheduler, exports, operator, login, sample) |
| Queue consumer and queued provider policy | `cloudflare/analysis/` |
| Routes | every standalone route under `src/app/api/`, new `src/app/api/operator/` |
| Researcher UI | `src/components/analysis/`, `InterviewDetail`, `StudyDetail`, `Dashboard`, `src/services/` |
| Release, installer, operator and inventory tooling | `scripts/cloudflare/`, `wrangler.jsonc`, `.github/workflows/ci.yml` |
| Tests | `tests/workers/`, `tests/cloudflare-artifact/`, `tests/cloudflare-restart/`, `tests/e2e-cloudflare/`, `tests/setup-cloudflare/`, plus unit and integration additions |
| Documents | `docs/operations/cloudflare-migration/{IMPLEMENTATION,RUNBOOK,INSTALLER,TRANSITION}.md`, `evidence/{M0-feasibility,DEVIATIONS,MEASUREMENTS,REVIEW-PACKET}.md`, `skills/openinterviewer-cloudflare/SKILL.md`, README, AGENTS, `/self-host` |

## 5. What was built (short)

- **Runtime.** One Worker: OpenNext `fetch`, an independent `queue` handler and the exported `WorkspaceStore` class. `DEPLOYMENT_TARGET=cloudflare` resolves capabilities centrally and production-strict; a Worker can never construct a Redis client. The admission identity comes only from a validated `CF-Connecting-IP` through `AsyncLocalStorage`; browser-supplied `x-openinterviewer-internal-*` headers are stripped. The OpenNext wrapper is replaced by a copy that honors response backpressure and fails the body on a stream error (DEVIATIONS RT-02/ST-08).
- **Storage.** A domain `WorkspaceStorePort` with a Redis adapter (existing behavior, shared contract scenarios) and a durable client over one SQLite-backed Durable Object per installation. All authority checks and multi-row effects happen inside object transactions, with the alarm committed in the same transaction. Every reply is validated against a closed union. Reader-compatible migrations (`min_reader_version`).
- **Durable analysis.** Completion allocates generation 1 in the save transaction. A single alarm scheduler dispatches identifier-only Queue messages; the consumer claims with a nonce and lease, loads only the frozen provider adapter, commits a start marker and calls the provider once with SDK retries disabled. A timeout, network loss or 5xx after start is recovery-required; only a researcher action (API v2 with `Idempotency-Key` and `expectedGeneration`) starts another paid attempt. A recovery epoch fences restored or imported work.
- **Operations.** Maintenance modes (`open`, `draining`, `frozen`, `recovery`), operational backup and import (format v1, manifest and trailer, resumable), epoch activation, a guarded PITR restore entry point, an operator API behind an `OPERATOR_TOKEN` plus a recent session, and an operator CLI.
- **Installation.** `setup:cloudflare plan|apply|resume|verify|update|config`, a deploy script that uploads only a checked artifact, a combined release check that writes the receipt deploy requires, a gated CI promotion job, and an agent skill that drives the installer.

## 6. API, configuration and schema changes

- **Analysis API v2** (Cloudflare): `GET /api/interviews/{id}/analyze` returns closed status bodies (`pending` with `phase` `queued`, `running` or `not-scheduled`, `complete`, `already-complete`, `failed` with `failureKind` and `recoveryRequired`); `POST` requires `X-OpenInterviewer-Analysis-Version: 2`, a UUID `Idempotency-Key` and `{expectedGeneration}`, and answers `409` with `ANALYSIS_CLIENT_UPDATE_REQUIRED`, `ANALYSIS_STATE_CHANGED` or `ANALYSIS_REQUEST_KEY_CONFLICT`. Node keeps the synchronous protocol. `/api/config/readiness` and `/api/config/status` report `analysisExecution` (`synchronous`, `queued-v2` or `null`).
- **Operator API** (Cloudflare only): `GET /api/operator/status`, `POST /api/operator/maintenance`, `GET /api/operator/backup`, `POST /api/operator/backup/import`, `POST /api/operator/recovery/activate`, `POST /api/operator/recovery/restore`. Each needs `Authorization: Bearer <OPERATOR_TOKEN>` and a researcher session issued within 15 minutes.
- **Configuration** (`wrangler.jsonc`): vars `DEPLOYMENT_TARGET`, `DEPLOYMENT_MODE`, `AI_TRANSPORT`, `AI_PROVIDER`, `APP_BASE_URL`, `WORKSPACE_ID`, `WORKSPACE_JURISDICTION`, `WORKSPACE_BOOTSTRAP` (installer-only); secrets `ADMIN_PASSWORD`, `SESSION_SECRET`, `PARTICIPANT_TOKEN_SECRET`, `RATE_LIMIT_SALT`, `OPERATOR_TOKEN`, `ANALYSIS_RECOVERY_EPOCH` and the one selected provider key; bindings `WORKSPACE_STORE` (SQLite DO, migration tag `v1`), `ANALYSIS_QUEUE` producer and consumer (batch 1, concurrency 1, 3 retries, 30 s delay, dead-letter queue). Invocation logs are off and query strings redacted.
- **Schema**: SQLite migration 1 (studies, interviews, analysis, analysis_jobs, participant_links, consents, idempotency_receipts, deletion_fences, budget windows and members, aggregates, workspace_meta, operator_audit, login_attempts) and the `schema_migrations` ledger with `min_reader_version`. IMPLEMENTATION.md §4.
- **Study list** (all targets, Codex round): `GET /api/studies` returns list items (metadata, `config.name`, `config.description`, `coreQuestionCount`), not whole configurations; Edit reads `GET /api/studies/[id]`. The port's `listStudies` returns `StudyListItem`; the Durable Object pages it by keyset (4 MiB of stored configuration per page requested, 12 MiB and 1,000 rows capped) and the Worker assembles at most 16 MiB, else 413.
- **Node-visible changes** are listed in DEVIATIONS.md (Node rows): loopback `APP_BASE_URL` rejection in Node production, `busy` stops a researcher batch, some error bodies, the study-scoped 413 copy, a failed refresh no longer empties the register.

## 7. Installer, update and restore behavior

- `plan` is read-only. `apply` records each phase only after observing its effect: queues, installation config, initial deploy, one bulk secret upload (four generated secrets plus the epoch; the admin password and one provider key from stdin or a no-echo prompt), origin (discovered from the Worker's `workers.dev` URL or given), workspace initialization through the Worker's own URL under `WORKSPACE_BOOTSTRAP`, bootstrap clear, verify. `resume` adopts a queue or Worker only with ownership evidence. `update` refuses drift, identity or jurisdiction changes and unready artifacts; exit 3 means deployed but held. `config` rewrites the installation config from the receipt without any remote call. A provider change (`update --change-provider`) is recorded as `pendingProviderChange` before its first remote write and finalized after its deploy; rerunning the same update finishes an interrupted one, and other commands refuse meanwhile.
- Deploy uploads only the prebuilt artifact whose manifest matches the clean checkout and whose receipt shows a passing release check; bootstrap values need `--bootstrap`. CI promotion (`workflow_dispatch`, `cloudflare-production` environment) checks the stored config, runs the full release check, deploys and verifies readiness; it does not update the installer receipt.
- Restore paths: operational backup and import into a fresh workspace in `recovery`, then epoch activation (all local-tested); PITR through the guarded restore entry point after deploying a new epoch first (refusals local-tested, the restore itself remote). RUNBOOK.md and TRANSITION.md hold the procedures. Operator state changes (maintenance, activate, restore, import) whose reply is lost, cut off, timed out or unrecognizable exit 1 with `detail.outcome: "unknown"` and a status read taken afterwards; they are never resent, except idempotent import requests.

## 8. Evidence highlights

- **Provider-request counts.** JOB-09 matrix in `tests/workers/jobFaults.test.ts`: under the queued policy each adapter (OpenAI, Anthropic, Gemini, OpenRouter) makes exactly one request for 429, 5xx, timeout and network failure; the fixture sends `retry-after-ms: 1`, so enabling SDK retries fails the test deterministically. Races: two researcher tabs and duplicate deliveries cause exactly one more request (workers tier and the Cloudflare browser lane). Each artifact provider journey makes exactly one greeting, one interview and one synthesis request.
- **Fault cuts.** 47 cuts in `tests/workers/faultManifest.ts` (write sites, RPC boundaries, the M0 probes), each with durable evidence, expected reply, next action and covering tests, checked by `tests/unit/cloudflareFaultManifest.test.ts`. The restart lane kills the whole runtime and restarts on the same storage: a committed save is analysed after restart with no client request; a lost reply replays as duplicate; a provider call in flight at the kill is never retried and becomes recovery-required after the lease; an uncommitted transaction leaves nothing.
- **Measurements** (MEASUREMENTS.md): artifact 27413.90 KiB upload, 5440.21 KiB gzip (64 MiB limit on both plans; the source map is not uploaded); local startup CPU 24.3 ms after lazy provider loading (measured at `80ec88d`; 116.5 ms before, 18.0 ms at M0); memory at the 500-record export and 1,000-record read maxima is not measured locally.

## 9. Remaining remote-only gates (not run)

1. A real deploy and upload of the checked artifact to a staging installation with customized names; install, interrupt and resume, repeated apply and update, and a deliberately failed release check that leaves production unchanged (SETUP-01/03/05/07, VERIFY-04).
2. Queue consumer registration and consumption, and alarm wake-up after inactivity, on Cloudflare (JOB-05/10, VERIFY-04).
3. Real ingress: `CF-Connecting-IP`, Pseudo IPv4 off, `CF-Worker` subrequests, custom-domain routing (RT-07).
4. EU jurisdiction placement (local workerd refuses jurisdictions) and the chosen jurisdiction's behavior.
5. PITR restore rehearsal including the epoch-first order and back-out (OPS-03).
6. Protected logs: invocation logs off and query-string redaction on the real account (RT-10).
7. CPU on the Free plan's 10 ms limit and real `startup_time_ms`; isolate memory for the 500-record export with a slow client, 16 MiB aggregate inputs and 16 MiB listings (ST-08, VERIFY-03 Boundaries).
8. One authorized paid smoke per provider actually used (named model, one invocation, retries off, metadata only).
9. GitHub Actions behavior of the new concurrency settings and the promotion job with a real environment and token.
10. Production transition (OPS-04): authorized metadata-only inventory of the old Upstash data, disposition decision, old-writer barrier rehearsal and switch.

## 10. Decisions for the reviewer and next authorized actions

Decisions:
- Sign-in budget keying for IPv6: full address as RT-07 states (current), or `/64` (DEVIATIONS F5/RT-07 open question).
- Wrangler-step minification (about −9 MB upload) and the upstream reports for `@vercel/og` in the middleware and the wrapper backpressure (MEASUREMENTS options; not implemented).
- Production data disposition: clean start or preserve (TRANSITION.md); an Upstash importer is built only if the inventory shows data to keep.
- Storage jurisdiction for the production installation (`eu` recommended), the final origin, and whether CI promotion becomes the production deploy owner.
- The generate-link request cap stays at 140,000 bytes until cached clients age out (DEVIATIONS).

Next concrete actions (steps 2 to 5 each need your explicit authorization):
1. Review this branch; rerun `npm run check:cloudflare` on the head commit.
2. Staging: `npm run setup:cloudflare -- plan --install <name> --env staging ...`, then `apply` with a staging provider key, then the staging report of VERIFY-04 (including the restart, Queue-after-inactivity and PITR rehearsals).
3. Paid smoke per provider as in gate 8.
4. Authorized `npm run inventory:redis` against the current Upstash database (metadata only), then the data-path decision.
5. Production install, CI promotion setup (`setup:cloudflare config` into `CLOUDFLARE_INSTALL_CONFIG`), transition per TRANSITION.md.

## 11. Independent review and its disposition

A seven-dimension adversarial review of the whole diff (security, paid-call boundary, durability, Node regressions, installer and deploy graph, UI/API contract, accuracy of claims), with a verifier trying to refute each finding, kept 18 findings (0 refuted; 2 major). All 18 were fixed with regression tests, and a second verifier round found smaller residual issues, which were fixed as well.

| Finding (severity after verification) | Fix |
| --- | --- |
| Push to main could cancel an in-flight production promotion (major) | Dispatch runs get their own non-cancelling concurrency group; static test of GitHub's rules |
| Study page emptied its register on a failed refresh or a Cloudflare list over 16 MiB (major) | Checked readers; state committed only on success; non-destructive notice |
| CI promotion bypassed the installer's checks (minor) | Config check before the release check, `--bootstrap` guard, readiness verification after deploy; documented as the maintained instance's owner |
| Installer and operator CLI accepted admin passwords sign-in would reject (minor) | One maximum in the route, readiness, setup checker, installer and CLI, tied by a test |
| Held-alarm re-arm claim false under identity mismatch (minor) | Scheduler re-arms hourly; epoch mismatch stays inert |
| Undocumented Node 413 copy change (minor) | Recorded in DEVIATIONS |
| `--skip-next-build` could label a stale build (minor) | Flag removed |
| Clean-checkout gate ignored untracked files (minor) | Untracked, non-ignored files make the checkout dirty |
| Boundary lane passed vacuously on encoded paths (minor) | `isMain()` with real-path fallback in every entry script |
| Wrangler rollback / epoch claims false without a TTY (minor, 2 findings) | RUNBOOK, IMPLEMENTATION, DEVIATIONS and comments corrected; rollback prohibition and re-check added |
| PITR procedure had no implementation (minor) | Guarded restore entry point (RPC, route, CLI) |
| Secrets file suggested inside the checkout (minor, 2 findings) | Docs use a path outside the checkout or a secret-manager pipe |
| Stale R2 statement in MEASUREMENTS (minor) | Corrected |
| `minReaderVersion` did not typecheck on real migrations (minor) | Field moved onto the production `Migration` type |
| CI promotion undocumented (minor) | INSTALLER, RUNBOOK, SKILL sections |

The second round fixed what the fix verifiers found: harness credential handling (scrub, not refuse), the operator CLI limit tie, setup-checker maximum, a `setup:cloudflare config` command, inventory counting and entry guard, the fault-manifest entry for the restore RPC, the Login length limit that would have locked out Node operators, the hosted 403 message, a direct test for internal-header stripping, and documentation precision. A final browser pass added the race, keyboard and keyless-demo tests and made a maintenance-hold refusal read as certain.

Known residual gaps (not defects found in review):

- The IPv6 sign-in key question, the Re-analyze disabled-reason design question and the other open rows in DEVIATIONS.md (Pending and open questions).
- Small test gaps a verifier's mutants found without a behavior defect: the release-check build step's scrubbed environment, the harness scrub-before-import order, the `config` command lock, and one inventory de-duplication condition.
- The inventory summary does not count a misfiled index entry whose interview belongs to another existing study (the per-family counter does; TRANSITION.md says to record both).
- `docs/operations/cloudflare-migration/03-analysis-jobs.md:100` has a table row with one extra column (a specification original, left unedited).
- The artifact was built under Node 26.9.0 locally; CI pins 24.19.0.

## 12. Codex review round (2026-09-24)

Codex reran `check:cloudflare` on `7235c80`: all 18 lanes passed. It then reported four reproducible P2 issues. Each was fixed with regression tests that fail on the previous code. Two verifiers checked each fix independently: one tried to reproduce the original failure, the other looked for regressions against the specifications. Their findings were fixed or documented before commit.

| Finding | Commit | Fix | Regression evidence |
| --- | --- | --- | --- |
| Large study lists fail: one RPC returned every study, and 300 valid large studies (37,365,331 bytes) exceeded workerd's 32 MiB RPC limit (503) | `655b426` | The list returns list items, not configurations, on every target. The object pages over (created_at DESC, id DESC) within a stored-byte budget: sizes are read first, a study larger than the budget is a page of its own, and the cursor advances past undecodable rows. The Worker assembles at most 16 MiB, else 413. A request without paging that would not fit is `too-large` at the object. Edit reads the full study first | `tests/workers/studyList.test.ts` (new) and `tests/workers/researcherRoutes.test.ts` (300 maximum-size studies answer 200 with every item): with the pre-fix source restored, 16 of these tests fail, and workerd reports "Serialized RPC arguments or return values are limited to 32MiB, but the size of this value was: 37365331 bytes"; unit tests for list items and Edit |
| Polling can hang: the 180 s budget was checked only between reads | `15713de` | Each read gets a timer of max(time left in the session, 30 s). An automatic read aborted at the deadline ends the budget as exhausted. A manual check that times out halts with a retryable error. A read made moot by a stored outcome is dropped without an error. Reads stay serial | `tests/unit/analysisExecution.polling.test.ts` (+8; 6 fail on the old code): a read that never resolves is aborted at the deadline, and a manual check then starts a new read |
| An interrupted provider change could not be resumed: config written before the deploy, receipt updated after | `5d37582` | `pendingProviderChange` is recorded after the read-only checks and key validation, before any upload or deploy. Drift accepts either side of a pending change only. The same update finishes it, with one history entry. `update`, `resume`, `apply` and `config` refuse other requests while a change is pending | `tests/setup-cloudflare/update-verify.test.mjs`: a failed deploy before the upload, a lost reply after it, and a lost key-upload reply are each rerun to completion; conflicting providers and unrelated drift are still refused |
| Operator commands mishandled lost connections: a committed transition was reported as a socket error | `2e72fe3` | Every state-changing request (maintenance, activate, restore, import chunk and finalize) treats a reset, cut-off, timed-out or unrecognizable reply as `outcome: "unknown"`. The request is never resent, except idempotent import requests. Status is read once and reported, or the report says status could not be read either. Maintenance and activation count a 200 as success only when it carries the documented result | `tests/setup-cloudflare/operator.test.mjs`: a fake server commits the change, then resets, truncates, returns HTML or hangs, for each state-changing command; a failed status read; a read-only command reports a plain error |

Decisions and residuals from this round, for review:

- **Study list shape (recorded in DEVIATIONS ST-08).** Full configurations cannot fit a 128 MB isolate at the 1,000-study maximum (up to about 125 MiB), however they are paged. The list therefore carries list items on Node, hosted and Cloudflare alike. Anything that scripted full configurations from `GET /api/studies` must read `GET /api/studies/[id]` instead, which is worth a release note. A browser tab still running the previous release's bundle fails to render the new list shape until it is reloaded (there is no deployment skew protection). It fails closed: it never prefills Edit from a partial configuration.
- **Keyset cursors.** Interview, aggregate and study cursors share one format (JSON `[created_at, id]`, bound only as SQL parameters, never sent to a browser). A malformed text or number key on a page boundary is skipped like any undecodable row. A NULL or BLOB key, possible only through storage corruption, still makes that listing unavailable.
- **Polling.** Inside a live session, a stalled read keeps "Checking…" until the deadline; the worst case for the batch is 210 s. A refresh during an outstanding read is still ignored.
- **Installer.** There is no abandon command for a pending provider change. The way back is to finish it, then change back (both keys stay bound). `verify` during a pending change can report an `AI_PROVIDER` mismatch.
- **Remote gates added.** Peak memory for a 16 MiB study list (ST-08/RT-09 row). A lost reply against real Cloudflare for operator commands and installer updates, covered by the staging rehearsal (VERIFY-04).

## Appendix A. Requirement-to-evidence map

Status: `met-local` implemented and proven by local tests; `remote-gate` implemented, acceptance needs a live account, provider or production data; `deviation` implemented differently or not delivered, recorded in DEVIATIONS.md; `partial` acceptance evidence still incomplete locally.

| ID | Status | Requirement | Evidence (tests by file and title, or documents) | Notes |
| --- | --- | --- | --- | --- |
| RT-01 | met-local | DEPLOYMENT_TARGET=node\|cloudflare (absent=node) resolved centrally with mode/transport; unsupported combos fail before storage/provider; no Redis path inside a Worker. | tests/unit/runtime.capabilities.test.ts > 'RT-01 resolves %s' / 'RT-01 refuses %s' / 'RT-01 refuses the Node default inside a Worker runtime' / 'RT-01 never infers storage from credentials or bindings-shaped values'<br>tests/unit/kvClient.workerFence.test.ts > 'RT-01 %s refuses in a Worker runtime regardless of env' / 'RT-01 covers every RedisPort method'<br>tests/unit/api.health.cloudflare.test.ts > 'RT-01 a Worker whose process env lost the target never falls back to Redis'<br>tests/cloudflare-artifact/runtime.artifact.test.ts > 'Worker target guard (RT-01)' > 'refuses to serve when the Worker is not declared as the cloudflare target'<br>(+1 more) | Nothing lint-level stops src/ from importing cloudflare/**; the Node build lanes would catch it, and grep finds no such import. The keyless /demo is only rendered in the Cloudflare smoke test, with no assertion that it makes no network calls. |
| RT-02 | met-local | One Worker: fetch→OpenNext, independent queue() handler, exported WorkspaceStore; WORKSPACE_STORE, ANALYSIS_QUEUE producer+consumer and SQLite migration declared. | tests/setup-checker.test.mjs > 'RT-02 Wrangler configuration must declare the SQLite store and analysis queue contract' / 'RT-02 the repository Wrangler template satisfies the binding contract'<br>tests/cloudflare-artifact/journeys.artifact.test.ts > `VERIFY-03/JOB-01 ${provider}: a participant saves and analysis completes in the background with exactly one synthesis request` (fetch, alarm, Queue and DO in the built artifact)<br>tests/cloudflare-restart/committed.restart.test.ts > 'VERIFY-01/JOB-05 a save committed before SIGKILL keeps its job and wake-up: ...'<br>release-check lane worker-import-boundary (receipt e42be63, exit 0) | DEVIATIONS: WORKER_SELF_REFERENCE is not bound, and the OpenNext wrapper is overridden with config validation disabled; tests/unit/openNextConfig.test.ts pins it. Real Queue consumer registration is VERIFY-04 (remote). |
| RT-03 | met-local | Locked feasibility gate: pinned OpenNext/Wrangler/Workers test tool plus compatibility date; prove proxy, cookie signing, transactional DO and Queue in one artifact; record size and startup. | docs/operations/cloudflare-migration/evidence/M0-feasibility.md<br>docs/operations/cloudflare-migration/evidence/MEASUREMENTS.md §1–4 (toolchain, 27,402.67 KiB upload, startup re-measured after R2)<br>tests/cloudflare-artifact/runtime.artifact.test.ts > 'RT-03 serves public pages and static assets from the built artifact' / 'RT-06 signs and verifies the researcher session inside workerd'<br>tests/e2e-cloudflare/smoke.spec.ts > 'public pages render and protected pages require sign-in' | DEVIATIONS decision: @cloudflare/vitest-plugin 1.2.3 replaces the pool package. The artifact was built locally under Node 26.9.0 (CI pins 24.19.0), so artifact bytes under 24.19.0 are unverified. Real startup_time_ms is a remote gate. |
| RT-04 | met-local | build/preview/test/typegen/setup/deploy:cloudflare commands; build never deploys; deploy consumes a previously checked artifact; non-secret manifest with hashes, schema range and test receipt. | tests/setup-cloudflare/deploy.test.mjs (19 verifyArtifact cases, bootstrap guard, --check-config)<br>tests/setup-cloudflare/release-scripts.test.mjs (build refuses unknown options; untracked files make the checkout dirty)<br>dist/cloudflare/artifact/manifest.json (source e42be63 dirty:false, schema {current:1,minReadable:1}) + receipt.json (passed, 17 lanes) — local, git-ignored<br>tests/setup-cloudflare/install.test.mjs > `apply refuses ${label} before any remote write` (label 'a failed release check')<br>(+2 more) | deploy.mjs verifyArtifact, bootstrap guard and --check-config are now tested directly (tests/setup-cloudflare/deploy.test.mjs); the real upload is remote. preview:cloudflare is a documented decision (synthetic providers only). |
| RT-05 | deviation | Validated runtime context from Worker env for HTTP, Queue and alarm; provider keys passed explicitly; never copy env into process.env; missing study provider key is a config failure, not a fallback. | tests/workers/consumer.test.ts > 'RT-05 reads keys from the current env only and never persists or logs them or participant content' / 'JOB-02/RT-05 records failed/provider without any provider request when the frozen provider key is missing'<br>tests/unit/hostedConfig.cloudflare.test.ts > 'RT-05 requires the selected provider key and never substitutes another provider'<br>tests/unit/api.auth.cloudflare.test.ts > 'RT-05 compares against ADMIN_PASSWORD from the Worker invocation env'<br>tests/cloudflare-artifact/journeys.artifact.test.ts > `JOB-02/RT-05 the stored ${provider} interview names the provider and the served model; no key or participant answer reaches the logs` | DEVIATIONS RT-05 (wording): the platform and OpenNext already populate process.env, so the rule is read as 'the app never writes it or treats it as authority'. M0 recorded process.env present in fetch, alarm and Queue, so the stated reason holds. |
| RT-06 | met-local | Preserve audiences, cookies and secrets; proxy tested in the artifact; APP_BASE_URL an explicit HTTPS origin (workers.dev valid), never from Host or XFF; private responses not cached. | tests/cloudflare-artifact/runtime.artifact.test.ts > 'RT-06 proxy protects researcher pages and clears an invalid session cookie'<br>tests/unit/appBaseUrl.test.ts > 'RT-06 requires a stable HTTPS origin inside any Worker runtime' / 'RT-06 accepts a workers.dev origin'<br>tests/unit/api.participant.cloudflare.test.ts > 'RT-06: persists only the code digest and builds the URL from APP_BASE_URL, never the request host' / 'RT-06: exchanges the code by digest, sets a tab-scoped HttpOnly cookie and is never cached'<br>tests/e2e-cloudflare/smoke.spec.ts > 'researcher signs in through the real session cookie path' | DEVIATIONS: the generate-link body cap is 140,000 bytes (tightening to 4 KiB is pending), and Node production now rejects every loopback origin. Canonical-origin Secure cookies over real HTTPS are VERIFY-04 (remote). |
| RT-07 | remote-gate | Server-owned admission identity from validated CF-Connecting-IP only, normalized; carried through the adapter and surviving the Next proxy; subrequests refused; unknown bucket; Pseudo IPv4 off; staging ingress validation. | tests/cloudflare-artifact/identity.artifact.test.ts<br>tests/workers/internalHeaders.test.ts<br>tests/unit/cloudflareWorkerEntry.test.ts<br>tests/unit/runtime.clientAddress.test.ts > 'RT-07 uses only the normalized CF-Connecting-IP' / 'RT-07 collapses equivalent IPv6 spellings to one full lowercase form' / 'RT-07 marks any CF-Worker subrequest regardless of its value or a valid address'<br>(+3 more) | Identity now proven through the built artifact (distinct CF-Connecting-IP values get separate budgets; forged internal headers do not change identity); withoutInternalHeaders has a direct workers test. Real ingress, Pseudo IPv4 and CF-Worker behavior are remote. Open question: IPv6 /64 keying for the sign-in budget (DEVIATIONS F5/RT-07). |
| RT-08 | met-local | Cloudflare readiness requires config, secrets, provider key, both bindings and a supported schema without Redis; bounded DO probe; 503 when not ready; analysisExecution advertised; no provider, dispatch or writes. | tests/unit/api.health.cloudflare.test.ts > 'RT-08 reports ready after a bounded workspace readiness check, with no Redis' / 'RT-08 bounds the workspace readiness RPC at 2 seconds' / 'RT-08 reports a missing Queue binding without probing the workspace'<br>tests/unit/api.config.mode.test.ts > 'RT-08 keeps advertising the protocol while not ready, and withholds it when unsupported'<br>tests/unit/api.studies.cloudflare.test.ts > 'RT-08: a not-ready deployment refuses every mutating and provider route before storage or provider use'<br>tests/cloudflare-artifact/journeys.artifact.test.ts > 'reports not ready with workspace_uninitialized and refuses a researcher write without initializing the object'<br>(+1 more) | DEVIATIONS decisions: maintenance counts as not-ready; a readiness call may complete the bootstrap; a missing OPERATOR_TOKEN is only a warning. The spec requires binding presence only; real Queue consumption is VERIFY-04. |
| RT-09 | remote-gate | Domain boundary on every storage-using standalone route; bounded aggregate/export inputs; demonstrate max inputs within Worker memory/CPU or use bounded streaming; never silently truncate. | tests/unit/api.studies.cloudflare.test.ts > 'RT-09: pages bounded current-revision inputs (never the full collection) and saves the result' / 'RT-09: inputs beyond the byte ceiling are an explicit 413, never a truncated analysis'<br>tests/unit/zipStream.test.ts > 'RT-09: applies backpressure, so buffered output stays bounded while the consumer is idle'<br>tests/workers/exports.test.ts > 'ST-08/RT-09: pages newest first by (created_at, id), then aggregates in first-seen order, into a JSZip-readable archive'<br>tests/workers/participantRoutes.test.ts > 'RT-09, ST-03: a revoked link refuses the session, including a replay of its committed save'<br>(+1 more) | Route integration and bounding are met locally. The 16 MiB aggregate and list ceilings rest on a Node V8 heap proxy. Peak isolate memory in workerd at the maximum inputs is listed as Pending (remote) in DEVIATIONS, so demonstrating that accepted maxima fit Worker memory and CPU is outstanding. |
| RT-10 | met-local | Structured allowlisted logs without bodies, keys, codes or content; invocation logs capturing /p/<token> disabled; harmless marker tests on runtime and error paths. | tests/setup-checker.test.mjs > 'RT-10 Wrangler observability must not capture participant URLs' / 'RT-10 Wrangler must not export request URLs through Logpush or Tail Workers'<br>tests/unit/requestLog.test.ts > 'RT-10 keeps Cloudflare events closed to addresses, message bodies and extra fields'<br>tests/workers/consumer.test.ts > 'RT-05 reads keys from the current env only and never persists or logs them or participant content' (500 error path, TRANSCRIPT_MARKER)<br>tests/unit/operatorAuth.test.ts > 'RT-10 logs each decision as one allowlisted operator.action event without content' | Artifact log checks cover the provider key and the participant answer, not participant link codes. Reviewing real Workers Logs ('protected logs') is VERIFY-04 (remote). |
| ST-01 | met-local | WorkspaceStorePort with Redis and DO adapters passing shared domain scenarios; Redis/Lua wire and crash-cut suites independent and passing; hosted saga unchanged. | tests/integration/workspaceStore.redis.contract.test.ts and tests/workers/contract.durable.test.ts running tests/contract/workspaceStoreScenarios.ts > 'ST-01: create returns the minted study; the same key and fingerprint replay it without minting again' / 'ST-01: replacing the config advances the revision and a stale expected revision conflicts'<br>tests/integration/redis.crashCuts.test.ts > 'manifest coverage' > 'every listed cut has a real-wrapper test'<br>tests/workers/durableClient.test.ts > 'ST-01: a committed-but-lost completion reply is reported as ambiguous, never created'<br>tests/unit/storage.redis.test.ts > 'ST-01: the standalone POST /api/studies route, now through the store, issues exactly the pre-store route's Redis calls and create arguments' | The branch does not touch platformDb*.ts or studyOperationReconciler.ts. Some scenarios are gated by capability flags; two it.skipIf apply to Redis. DEVIATIONS ST (create, Node body drift) documents three changed Node error bodies. |
| ST-02 | met-local | Concurrent same/different-fingerprint completions: one immutable transcript, one charge, one initial job, correct count; lost-response replay without duplication. | tests/workers/completion.test.ts > 'ST-02: concurrent same-fingerprint completions yield one transcript, one charge, one job and a correct count' / 'ST-02: concurrent different-fingerprint completions for one interview id yield one transcript and a conflict' / 'ST-02: a lost-response replay returns duplicate without a second job, charge, alarm change or mutation'<br>tests/cloudflare-restart/committed.restart.test.ts > 'VERIFY-01/ST-02 lost save reply: the object commits, the runtime dies before the client reads the reply, and the same save replayed after restart is a duplicate with one interview, one job and one synthesis request'<br>tests/contract/workspaceStoreScenarios.ts > 'ST-02: concurrent identical completions store one transcript, charge the save budget once and count once' | DEVIATIONS decision: authority is checked before duplicate detection, so a replay after revoke or edit is refused (Redis parity). |
| ST-03 | met-local | Edit/revoke/expire/delete races refuse unauthorized writes; consent timestamp and expiry never renew on replay. | tests/workers/completion.test.ts > 'ST-03: an edited study refuses a save pinned to the old revision' / 'ST-03: a revoke issued once the save has read its link is applied only after the save commits' / 'ST-03: a link past its absolute expiry refuses the save although no cleanup ran'<br>tests/workers/participants.test.ts > 'ST-03: consent is first-writer-wins and a replay never renews acceptance time or absolute expiry'<br>tests/workers/studies.test.ts > 'ST-03: replaceStudyConfig is an expected-revision compare-and-set that preserves count and lock' | DEVIATIONS tightening: completion re-checks link, revision and consent inside the transaction. |
| ST-04 | met-local | Real local Worker transaction/alarm failure cuts prove no committed nonterminal job lacks recovery, including object restart and duplicate save. | tests/workers/completion.test.ts > 'ST-04/JOB-05: a failed generation allocation rolls back every completion write and arms no alarm' / 'ST-04/JOB-05: a failure while registering the alarm rolls back the SQL already written' / 'ST-04/JOB-05: an object restart preserves the committed completion, its job and its wake-up'<br>tests/cloudflare-restart/transaction.restart.test.ts > 'VERIFY-01/JOB-05 SIGKILL while a transaction holding SQL and an earlier alarm is still open leaves neither after restart; the committed row and its own alarm survive and fire unprompted' | Workers-tier 'restart' is evictDurableObject (same process). The restart lane covers a real process kill; the open-transaction kill runs on a probe object (txProbe.worker.js). |
| ST-05 | met-local | Malformed analysis/identity leaves records unchanged with sanitized corrupt diagnosis; legacy absence and valid exhausted counters behave distinctly. | tests/workers/reads.test.ts > 'ST-05: a legacy record without an analysis row reads as stored, distinct from malformed state' / 'ST-05: a valid exhausted attempt counter reads normally while a malformed counter is corrupt' / 'ST-05: malformed analysis state refuses the collection instead of hiding an intact transcript'<br>tests/workers/participants.test.ts > 'ST-05: a malformed consent row reads as missing with a corrupt diagnosis and is never overwritten by a replay'<br>tests/workers/analysis.test.ts > 'ST-05 refuses a corrupt frozen input without claiming' | DEVIATIONS decision: a malformed immutable record is skipped in lists (Redis parity), while malformed analysis state gives 503. |
| ST-06 | met-local | Budget denial mutates no scope; concurrent admissions and fixed-window duplicate saves preserve existing limits. | tests/workers/participants.test.ts > 'ST-06: a denial reports the 0-based rejected row and mutates no scope' / 'ST-06: concurrent admissions never exceed the maximum' / 'ST-06: a window opens at first consumption, never slides and restarts after it expires'<br>tests/workers/completion.test.ts > 'ST-06: a denied save charges no admission row, even the ones that had room' / 'ST-06: concurrent saves by different sessions never exceed a fixed-window maximum'<br>tests/contract/workspaceStoreScenarios.ts > 'ST-06: save admission refuses past a budget without charging any row, and a duplicate never charges twice' | DEVIATIONS tightening: the DO checks and charges in one transaction. Redis overshoot remains a documented residual. |
| ST-07 | met-local | Tombstoned deletion survives restart; delayed completion or Queue delivery cannot resurrect; sample clear cannot affect real studies. | tests/workers/studies.test.ts > 'ST-07: deletion cascades links, consent and aggregate, fences the id through restart and marks the receipt'<br>tests/workers/sample.test.ts > 'ST-07: clear never touches a real study or a real interview even when their ids are passed' / 'ST-07: a delayed save into a cleared fixture study cannot resurrect it, even after a re-seed'<br>tests/workers/consumer.test.ts > 'JOB-10 acknowledges a queued delivery for a deleted interview without resurrecting it' | DEVIATIONS ST-07: the Node seed collision check moved to fixture ids, a Cloudflare-only 409 SAMPLE_HAS_PARTICIPANT_DATA was added, and an ambiguous clear now returns 503. |
| ST-08 | deviation | Max payloads, collection overflow, Unicode/empty/provenance round trips and paginated exports in workerd with measured bounded memory; concurrent export mutation fails, never a valid partial archive. | tests/workers/completion.test.ts > 'ST-08: the maximum valid submission (about 512,000 bytes) persists and reads back intact' / 'ST-08: Unicode, empty shapes, optional legacy fields and absent provenance round-trip byte-for-byte'<br>tests/workers/exports.test.ts > 'ST-08: a streamed export survives concurrent collection, and a captured-row deletion errors it without finalizing' / 'ST-08: honours pageSize and maxPageBytes (measured) and still progresses past an oversized record'<br>tests/workers/durableClient.test.ts > 'ST-08: records near the 512,000-byte save cap are too-large past the 16 MiB ceiling; the client stops within one row of it'<br>tests/unit/api.export.durable.test.ts > 'ST-08: an archive stream that errors after the headers is a failure, never a partial download'<br>(+1 more) | DEVIATIONS ST-08/F1: a captured snapshot means only deletion or replacement of captured content invalidates an export, not any workspace mutation. The stated reason (avoiding livelock during collection) holds, since studyName comes from immutable interview rows. Also partial: 'measured bounded memory' in workerd is Pending (remote), because the ceilings rest on a Node heap proxy. The 16 MiB list ceiling makes 1,000 long interviews an explicit 413 (documented tightening). |
| ST-09 | met-local | Fresh schema, interrupted migration, unsupported future schema and compatible N−1→N→N−1 exercised with pending jobs. | tests/workers/schema.migrations.test.ts > 'an interrupted migration leaves no partial schema or ledger row and is retried on the next start' / 'an unsupported future schema refuses readiness, reads, mutations and dispatch, and keeps a wake-up' / 'N−1 → N → N−1 → N keeps serving pending and completed jobs and N\'s data'<br>tests/workers/workspace.bootstrap.test.ts > 'migrates a fresh object, adopts the configured identity and epoch, and reports ready'<br>tests/workers/faultCuts.test.ts > 'ST-09: an object whose migrations committed but whose metadata insert never ran initializes it on the next start without re-running migrations' | Only migration 1 exists, so N−1→N uses a synthetic additive migration. min_reader_version is a documented decision. |
| ST-10 | met-local | Portable backup/import reproduces counts, checksums, references and original expiries without resuming uncertain paid work; a fresh namespace cannot access another installation. | tests/workers/backup.test.ts > 'ST-10: import reproduces counts, checksums, references and original expiries; duplicate chunks are idempotent' / 'JOB-10/ST-10: imported nonterminal jobs stay held until activation reconciles them to recovery-required' / 'ST-10: import refuses a backup whose epoch was not rotated, and finalize refuses dangling references'<br>tests/workers/workspace.bootstrap.test.ts > 'refuses an object selected under another name'<br>tests/unit/backupFormat.test.ts > 'OPS-02: JSON types, Unicode and absolute (expired) lifetimes survive a file round trip' | No single local test runs CLI→operator route→real DO for export and import. The CLI is tested against a fake server, the routes against a fake store, and the DO directly. Staging rehearsal is remote. |
| JOB-01 | met-local | One storage authority: provider HTTP in the Queue handler outside the DO and transactions; save atomically persists transcript and initial job; no after() on Cloudflare. | tests/workers/completion.test.ts > 'JOB-01/JOB-05: commits transcript, analysis projection, initial job, admission, count/lock and alarm together'<br>tests/unit/api.participant.cloudflare.test.ts > 'JOB-01/02: persists once with consent binding and frozen explicit-model inputs; no after() and no provider'<br>tests/workers/consumer.test.ts > 'JOB-01/05 dispatches, claims, starts, calls the frozen provider once and attaches actual provenance'<br>tests/e2e-cloudflare/research-workflow.spec.ts > 'participant saves before analysis runs; background analysis completes after the tab closes; researcher recovers a failure once, exports and aggregates' | Also covered in the built artifact per provider and across SIGKILL (restart lane). |
| JOB-02 | met-local | Generation/jobId identity and frozen inputs (config, revision, provider, explicit model); keys read only at execution; missing key is recorded as a coarse failure without a request. | tests/workers/consumer.test.ts > 'JOB-02 executes the frozen provider, model and revision after the study is edited before claim' / 'JOB-02 keeps the frozen revision when the study changes between the provider result and attachment' / 'JOB-02 records failed/provider before the start marker, without any provider request, when the frozen model is no longer supported'<br>tests/workers/completion.test.ts > 'JOB-02: the initial job freezes the stored configuration of the verified revision, never a differing caller copy'<br>tests/unit/api.analyze.durable.test.ts > 'API-01/JOB-02: accepts with 202 and freezes the canonical study config, revision, provider and explicit model' | DEVIATIONS decisions: nonterminal analysis without a job projects recovery-required; a missing key or model gives failed/provider. |
| JOB-03 | met-local | State machine pending→claimed→started→complete/failed; expired started becomes recovery-required, never pending; terminal states never reopen; projection adds generation and recoveryRequired. | tests/workers/analysis.test.ts > 'API-02/JOB-03 projects recovery-required as timeout with recoveryRequired true' / 'JOB-03 records oversized, invalid-provenance and uncertain outcomes as their terminal classes'<br>tests/workers/scheduler.test.ts > 'JOB-08 marks a started job whose process died recovery-required after the lease, never pending'<br>tests/e2e-cloudflare/research-workflow.spec.ts > 'a synthesis 5xx after start needs recovery; one explicit retry makes exactly one more provider request' | DEVIATIONS decision: HTTP 5xx after start is recovery-required. That matches the spec's closed list of known failures. |
| JOB-04 | met-local | At most one active generation per interview; retry allocation checks, scoped key receipts, 409 on key reuse with different intent; racing keys never create a competing paid attempt. | tests/workers/analysis.test.ts > 'JOB-04 races an initial job and two retry keys to a single active generation and one provider request' / 'JOB-04 lets exactly one of two racing keys allocate after a terminal failure'<br>tests/workers/durableClient.test.ts > 'JOB-04: retry digests are scoped by workspace, study, interview and key, and bound to the expected generation'<br>tests/unit/api.analyze.durable.test.ts > 'JOB-04: replaying the same key and body reuses its receipt; the same key with another generation conflicts' | DEVIATIONS tightening: a retry that races a revision edit returns state-changed. |
| JOB-05 | met-local | No committed nonterminal job lacks a durable wake-up: transaction(async){SQL; await setAlarm} proven with rollback, response-loss and restart tests. | tests/workers/analysis.test.ts > 'JOB-05 rolls back the whole retry allocation when its alarm cannot be committed' / 'JOB-05/API-01 keeps a committed allocation and its wake-up across an object restart before the reply'<br>tests/workers/faultCuts.test.ts > 'M0: a throw after SQL and an awaited setAlarm inside storage.transaction rolls back both on the WorkspaceStore storage'<br>tests/cloudflare-restart/committed.restart.test.ts > 'VERIFY-01/JOB-05 a save committed before SIGKILL keeps its job and wake-up: after restart, with no client request, the alarm dispatches, the Queue consumer runs and the analysis completes with one synthesis request' | Alarm delivery after real inactivity is VERIFY-04 (remote). |
| JOB-06 | met-local | One alarm scheduler: commits the recovery due time before the Queue send, identifier-only envelope, ≤25 rows per alarm, validates the closed envelope, acks invalid, stale or terminal deliveries. | tests/workers/scheduler.test.ts > 'JOB-06 sends one identifier-only envelope after committing its reservation, then records the send' / 'JOB-06 processes at most 25 due rows per alarm and re-arms immediately for the rest' / 'JOB-06/07 keeps the reservation and its backoff durable when the send acknowledgement is lost'<br>tests/workers/consumer.test.ts > 'JOB-06 acknowledges malformed, foreign-workspace and foreign-epoch envelopes without writes' / 'JOB-06 acknowledges duplicate and out-of-order deliveries without a second provider call or write' | DEVIATIONS F13 decision: corrupt due rows are quarantined in memory. |
| JOB-07 | met-local | Consumer batch 1, concurrency 1, 3 retries at 30 s, DLQ; ≤16 dispatch attempts or 24 h pre-start; backoff; watchdog at 5 min; alarm failure persists a retry alarm. | tests/setup-checker.test.mjs > 'RT-02 Wrangler configuration must declare the SQLite store and analysis queue contract' (max_batch_size, max_concurrency, max_retries, retry_delay, dead_letter_queue)<br>tests/workers/scheduler.test.ts > 'JOB-07 records failed/storage and stops after 16 dispatch attempts' / 'JOB-07 records failed/storage 24 hours after allocation even with budget left' / 'JOB-07 persists a retry alarm before returning when storage fails during the alarm'<br>tests/workers/consumer.test.ts > 'JOB-07 sends an unknown message version to the dead-letter path without any RPC or provider call' / 'JOB-07 retries transport, without a provider call, when the claim outcome cannot be confirmed' | DEVIATIONS F6: a live consumer backlog defers without charging. Sustained scheduler failure shows only as per-alarm analysis.job logs plus operator status (oldestActiveAgeMs, scheduled alarm); /api/health/ready carries no scheduler signal. Real Queue retry, DLQ and retention behavior is remote. |
| JOB-08 | met-local | Fresh claim nonce, 180 s lease, 120 s deadline; start marker confirms lease before provider HTTP; response-loss table (claim, start, attach, crash) handled with no second provider call. | tests/workers/consumer.test.ts > 'JOB-08 replays a lost claim reply with the same nonce and never lets a second invocation call the provider' / 'JOB-08 does not call the provider when the start marker cannot be confirmed' / 'JOB-08 recovers a lost attach reply from the terminal receipt without calling the provider again'<br>tests/workers/analysis.test.ts > 'JOB-08 requires enough lease for the deadline plus attach margin before the start marker' / 'JOB-08/F14 rejects a result after the lease on the object clock when the caller clock runs behind'<br>tests/cloudflare-restart/lease.restart.test.ts > 'VERIFY-01/JOB-08 a job whose provider call was in flight at SIGKILL is never retried: after restart the lease watchdog records recovery-required and the provider saw one synthesis request' | DEVIATIONS F14 tightening: lease decisions use the object clock. |
| JOB-09 | met-local | queued-synthesis policy with zero SDK retries per adapter; timeout, 429, 5xx and transport fixtures prove exactly one outbound request; provenance mandatory; no fallback. | tests/workers/jobFaults.test.ts > 'JOB-09 queued synthesis through the %s adapter' > 'JOB-09 $name makes exactly one outbound request and settles per the classification table' (4 adapters × 10 behaviours)<br>tests/workers/jobFaults.test.ts > 'JOB-09 %s retries a 500 under the default policy but not under queued-synthesis' (control)<br>tests/unit/providers.queuedPolicy.test.ts > 'JOB-09 disables SDK retries per call and applies the explicit deadline under queued-synthesis' / 'JOB-09 leaves participant greeting and interview calls on the default policy'<br>MEASUREMENTS.md §5 mutant runs (removing retries:{strategy:'none'} fails 3 of 3) | Live-provider compatibility is a separately authorized remote gate. |
| JOB-10 | met-local | Retention (7-day receipts, 30-day terminal pruning, nonterminal never expires); deletion cancels jobs; external recovery epoch checked on every entry; activation reconciles restored nonterminal jobs to recovery-required. | tests/workers/operator.test.ts > 'JOB-10/OPS-03: activation reconciles every nonterminal generation to recovery-required, then old envelopes, claims and results are rejected' / 'JOB-10: activation refuses an unset configured epoch and never adopts one from the request'<br>tests/workers/scheduler.test.ts > 'JOB-10 deletes only expired rows and prunes old terminal detail but never the current or active generation' / 'JOB-10 leaves a restored alarm inert under an epoch mismatch and does not re-arm'<br>tests/workers/analysis.test.ts > 'JOB-10 replays a receipt with the current outcome and an expired receipt cannot allocate over another generation' / 'JOB-10 never resurrects a deleted interview from a late claim or result' | An actual PITR restore under a new epoch is OPS-03 (remote). |
| API-01 | met-local | POST analyze requires the v2 header, UUID Idempotency-Key and {expectedGeneration}; missing version gives 409 before mutation; researcher-only authority; unknown commit gives 503 retryable; mismatched terminal generation gives 409. | tests/unit/api.analyze.durable.test.ts > 'API-01: a %s version header is 409 ANALYSIS_CLIENT_UPDATE_REQUIRED before any workspace call' / 'API-01: a participant cookie or participant token never authorizes analysis (POST and GET)' / 'API-01: a thrown allocation RPC is 503 retryable, never success or failure'<br>tests/workers/analysis.test.ts > 'API-01/JOB-04 allocates generation 1 for a legacy record with expectedGeneration 0, armed and receipted' / 'API-01 refuses a mismatched terminal expectedGeneration and a stale study revision without allocation'<br>tests/unit/api.analyze.authority.test.ts > 'API-01 ignores the additive v2 header, key and body and still runs one synchronous analysis' | The spec chain 'legacy no-job GET → generation-1 allocation → completion' is covered in pieces (GET not-scheduled, generation-0 allocation, consumer completion of retry-allocated jobs), not as one scenario. The expired key is covered by the JOB-10 receipt test. |
| API-02 | met-local | Read-only GET with closed projection (pending/complete/already-complete/failed + recoveryRequired), no-store, no allocation; legacy generation 0 is not-scheduled; imported uncertainty is recovery-required. | tests/unit/api.analyze.durable.test.ts > 'API-02: returns the closed projection for %s, read-only and uncached' / 'API-02: maps %s without any mutation'<br>tests/workers/analysis.test.ts > 'API-02 reports an eligible legacy record as not-scheduled generation 0 and a legacy synthesis as complete' / 'API-02/JOB-04 projects attempted generation-0 pending state as recovery-required, never not-scheduled (F7)'<br>tests/unit/analysisApi.test.ts > 'API-02: parses the closed projection %j and nothing else'<br>tests/e2e-cloudflare/research-workflow.spec.ts > 'a synthesis 5xx after start needs recovery; one explicit retry makes exactly one more provider request' (GET projection assertion) | DEVIATIONS tightening: only completed records are analysable. |
| API-03 | met-local | Serial polling at 2 s to 30 s, then 5 s to 180 s; clamp 2–10 s; pause when hidden or offline; cancel on unmount; generation adoption and monotonicity; stable action key across uncertain retries. | tests/unit/analysisExecution.polling.test.ts > 'API-03: reads serially on schedule through the 180 s budget, then stops with work still pending' / 'API-03: pauses while hidden and reads once on return, within the remaining budget' / 'API-03: keeps the key and body across uncertain retries of one action'<br>tests/unit/InterviewDetail.durableAnalysis.test.tsx > 'API-03: an uncertain start is retried with the same key and body; a later action gets a fresh key' / 'API-03: unmounting stops polling'<br>tests/unit/analysisState.test.ts > 'API-03: never regresses a persisted outcome to pending within a generation' | Unit (jsdom) tier. The two-tab case is in the Cloudflare browser lane. |
| API-04 | met-local | Batch keeps the 25 cap and sequential order; waits for a persisted terminal outcome; failures continue; request errors stop; budget exhaustion stops and shows pending separately; Node busy stops as awaiting. | tests/unit/StudyDetail.durableBatch.test.tsx > 'API-04: waits for each persisted outcome before the next, continues after a recorded failure, and never counts accepted work' / 'API-04: polling budget exhaustion stops before the rest and names the still-pending interview separately' / 'API-04: a request error stops immediately and keeps the loaded register'<br>tests/unit/StudyDetail.analysisBatch.test.tsx > 'API-04: a busy interview stops the Node batch as awaiting and is not counted' | DEVIATIONS: the Node busy behavior changed, as API-04 requires. The UI-CF-04/F8 decision observes rather than POSTs to active work. |
| UI-CF-01 | met-local | Scope: InterviewDetail, StudyDetail, analysisApi and analysisState extended with existing primitives; participant Synthesis receipt, demo and preview unchanged. | tests/unit/InterviewDetail.durableAnalysis.test.tsx > 'UI-CF-01: a finished reading does not wait for the analysis capability' / 'UI-CF-01: unfinished work waits for the capability before offering any action'<br>tests/e2e-cloudflare/research-workflow.spec.ts > 'researcher preview never creates research records or jobs' | src/components/Synthesis.tsx is unchanged in the diff. |
| UI-CF-02 | met-local | State/copy table: not-scheduled, queued, running, complete, ordinary failure, recovery-required with paid disclosure, unreadable status, poll budget ended, old client. | tests/unit/InterviewDetail.durableAnalysis.test.tsx > 'UI-CF-02: an unscheduled legacy interview offers Run analysis and is never polled' / 'UI-CF-02: recovery-required discloses the paid request inline and offers Run analysis again' / 'UI-CF-02: an unreadable status keeps the last confirmed state and offers a read-only refresh' / 'UI-CF-02: when the polling budget ends the work stays pending, with a read-only refresh and no automatic start' / 'UI-CF-02: an old client is told to reload before starting analysis'<br>tests/e2e-cloudflare/research-workflow.spec.ts > 'a synthesis 5xx after start needs recovery; one explicit retry makes exactly one more provider request' | Each row has a component test. The recovery row is also covered in the browser on the artifact. |
| UI-CF-03 | met-local | One key per intentional action; mounts and polls read only; capability-selected protocol (absent means legacy); no client-side lease assumption; cancel on unmount or study change. | tests/unit/analysisExecution.test.ts > 'UI-CF-03: readiness %j selects %s; only an explicit queued-v2 selects the durable protocol' / 'UI-CF-03: a readiness request that never answers ends as unknown after a bounded wait'<br>tests/unit/InterviewDetail.durableAnalysis.test.tsx > 'UI-CF-03: never polls, and starts the legacy way that a durable server refuses before any work' / 'UI-CF-02: running work has no retry, however old the browser thinks the claim is'<br>tests/unit/StudyDetail.durableBatch.test.tsx > 'API-03: leaving the study stops observing the batch' |  |
| UI-CF-04 | met-local | Batch: oldest-first ≤25, progress only on confirmed terminal outcome, recovery disclosure before a batch including unknown outcomes, never an automatic retry, keeps the loaded register on refresh failure. | tests/unit/StudyDetail.durableBatch.test.tsx > 'UI-CF-04: recovery-required interviews in the batch disclose the paid request before the action' / 'UI-CF-04: an outcome observed as recovery-required during the batch is counted and never retried by it' / 'UI-CF-04: a refresh that meets a pending study operation keeps the loaded register' | Unit tier only; see UI-CF-05 for the missing real-backend batch. |
| UI-CF-05 | met-local | Component tests of every row; two tabs plus detail/batch against the real backend resolve to one generation with provider counts; tab-close completion; live region and alerts; keyboard; 375px; design lint. | tests/e2e-cloudflare/analysis-controls.spec.ts (3 tests)<br>tests/e2e-cloudflare/research-workflow.spec.ts > 'participant saves before analysis runs; background analysis completes after the tab closes; researcher recovers a failure once, exports and aggregates' (two detail tabs → generation 2, synthesis count +1, 375px checks)<br>tests/unit/InterviewDetail.durableAnalysis.test.tsx > 'UI-CF-05: unchanged polls neither re-announce nor move focus' / 'UI-CF-05: a polled completion on a cited-turn page keeps the Analysis tab and focus'<br>tests/unit/InterviewDetail.analysis.test.tsx > 'UI-CF-05: a completed analysis refreshes in place and is announced once, politely' | Browser tier on the artifact now covers the detail + batch + automatic-job race with exact provider counts, keyboard-only operation with focus rings, disabled states that state why, and focus stability under polling. Open design question: "Re-analyze All Interviews" disabled after a study edit without a visible reason (rule N7.3; DEVIATIONS). |
| SETUP-01 | remote-gate | Checked-in setup:cloudflare plan/apply/resume/verify/update with receipts, derived names, no duplicates on resume, drift stop, secrets via stdin only. | tests/setup-cloudflare/install.test.mjs > 'plan is read-only and reports a new installation' / 'interrupted resources phase (before the DLQ is created) resumes without duplicate queues' / 'secrets uploaded but reply lost: resume keeps them and never regenerates the epoch' / 'apply and resume on a completed installation are no-ops'<br>tests/setup-cloudflare/update-verify.test.mjs > 'update requires an explicit existing-install identity' / 'plan on an installed system reports reuse, refused changes and drift without writing'<br>tests/setup-cloudflare/units.test.mjs > 'ownership evidence: a receipt attempt alone never adopts an existing resource' | Tested only against a fake wrangler. INSTALLER.md 'Not verified' lists real wrangler output parsing (queue table, error codes, deployments annotation), real uploads and DO bootstrap. These are the VERIFY-04 staging rehearsal. |
| SETUP-02 | deviation | Fresh install generates independent 32-byte SESSION_SECRET, PARTICIPANT_TOKEN_SECRET and RATE_LIMIT_SALT; admin password ≥16 characters; exactly one provider key; reject blank, template or reused values; explain billing. | tests/setup-checker.test.mjs > 'SETUP-02 Cloudflare requires exactly the selected provider key' / 'SETUP-02 Cloudflare rejects placeholder and reused credentials'<br>tests/setup-cloudflare/units.test.mjs > 'generated identities and secrets have the required shapes' / 'supplied credentials are validated without echoing values'<br>tests/setup-cloudflare/install.test.mjs > `apply refuses ${label} before any remote write` (short password, template password, reused credential, unexpected credential name)<br>tests/setup-cloudflare/install.test.mjs > 'plan is read-only and reports a new installation' (asserts billing notes) | DEVIATIONS F5: a fourth generated secret, OPERATOR_TOKEN, is written to a file outside the repository. ANALYSIS_RECOVERY_EPOCH is also generated and bound as a secret (F4). |
| SETUP-03 | remote-gate | User-confirmed HTTPS origin, or authenticated workers.dev discovery; not-ready until a second configuration deploy; no anonymous bootstrap endpoint; origin changes are explicit. | tests/setup-cloudflare/install.test.mjs > 'fresh apply creates resources once, uploads every secret in one call and bootstraps the origin and workspace' / 'a deploy script that refuses an empty APP_BASE_URL stops discovery cleanly and resume --origin completes' / 'an explicit workers.dev origin that the deploy does not confirm is refused until corrected'<br>tests/setup-cloudflare/units.test.mjs > 'workers.dev discovery reads only the triggers block and only this Worker\'s own URL'<br>tests/setup-cloudflare/update-verify.test.mjs > `update refuses ${label} without deploying` (label 'an origin change') | DEVIATIONS RT-04/SETUP-03 decision: empty APP_BASE_URL is allowed only with WORKSPACE_BOOTSTRAP set. The real deploy output URL format and workers.dev subdomain need a live account. |
| SETUP-04 | deviation | README and /self-host instructions plus a tested Deploy to Cloudflare link and template (selected-provider key only, secret-example precedence, renamed resources); publish only after an empty-account journey passes. | docs/operations/cloudflare-migration/evidence/DEVIATIONS.md (SETUP-04)<br>README.md: 'A one-click Deploy to Cloudflare button is not offered yet: it will be published only after a complete fresh-account installation has been tested.'<br>No .dev.vars.example, deploy-button template or binding descriptions in the tree (ls of repo root; .env.example only adds a pointer) | No Deploy to Cloudflare button or template: a button deploy cannot run the installer secret generation, origin/workspace bootstrap or release gates, and Workers Builds conflicts with SETUP-05 single ownership. The guided installer is the supported path; the button stays unpublished (DEVIATIONS SETUP-04). |
| SETUP-05 | remote-gate | One deployment owner: a GitHub Actions promotion job dependent on the full matrix for the exact source and artifact; Workers Builds and preview URLs disabled; staging isolated. | tests/setup-cloudflare/update-verify.test.mjs > `update refuses ${label} without deploying` (label 'a failed release check (production unchanged)')<br>tests/setup-cloudflare/install.test.mjs > 'staging uses separate names, state, identity and origin'<br>tests/setup-cloudflare/units.test.mjs > 'names are derived once per environment and staging never shares a name' | promote-cloudflare runs on workflow_dispatch from main, needs [quality, browser, redis-crash, adversarial, cloudflare], re-runs check:cloudflare (including the restart lane) and deploys that artifact. The CI 'cloudflare' job itself skips the restart lane. Disabling Workers Builds is a manual instruction. The GitHub environment and a deliberately failed release check through CI are VERIFY-04 (remote). |
| SETUP-06 | met-local | Repository agent skill driving the checked-in scripts (plan, apply/resume, verify), obeying credential rules, with no implicit deletes, DNS changes or paid smokes; handoff reports URL, version, receipt and limitations. | skills/openinterviewer-cloudflare/SKILL.md (credential policy l.13, prohibitions l.14, verify and hand-off §5 l.69–81)<br>tests/setup-cloudflare/*.test.mjs (the scripts the skill drives) | This is a documentation deliverable. No agent run of the skill has been exercised end to end. |
| SETUP-07 | remote-gate | Acceptance receipt: fresh renamed install, synthetic save → Queue analysis → export, no Redis traffic, clean repeat apply/update, interrupted recovery, missing binding/origin failures, failed release check leaves production unchanged; manual steps recorded. | tests/setup-cloudflare/update-verify.test.mjs > 'verify reports a ready installation and is read-only' / `verify fails clearly on ${label}`<br>tests/setup-cloudflare/units.test.mjs > 'readiness evaluation distinguishes ready, held, not-ready and Redis leakage'<br>tests/cloudflare-artifact/journeys.artifact.test.ts > `ST-08 the researcher export of the ${provider} workspace is a complete archive`<br>INSTALLER.md 'Manual steps outside the installer' and 'Not verified' | The pieces are exercised locally against a fake account and the local artifact. The acceptance receipt itself needs a live account and has not been produced. |
| OPS-01 | remote-gate | Persisted maintenance modes (open, draining, frozen, recovery) enforced by all handlers and store methods including Queue callbacks; authenticated CAS transitions with audit; rehearsed barrier for old Vercel/Redis writers. | docs/operations/cloudflare-migration/TRANSITION.md §6<br>tests/workers/operator.test.ts > 'OPS-01: compare-and-set transitions follow the allowed graph, resolve lost replies and audit without content' / 'OPS-01: freezing with in-flight attempts requires explicit classification (claimed → pending, started → recovery-required)'<br>tests/workers/participantRoutes.test.ts > 'OPS-01: draining refuses new collection and link creation but lets a started session continue; frozen refuses the session'<br>tests/workers/scheduler.test.ts > 'OPS-01/F11 stops dispatch and cleanup when a freeze commits while a Queue send is in flight'<br>(+2 more) | Cloudflare side met locally. The old-writer barrier is chosen and specified in TRANSITION.md §6 (with inventory comparison); its rehearsal and execution need authorization. |
| OPS-02 | met-local | Versioned operational backup from a frozen workspace (manifest, counts, SHA-256 chunks, trailer, watermark per page, no secrets); resumable, idempotent import into an empty recovery workspace with full validation. | tests/workers/backup.test.ts > 'OPS-02: a frozen workspace exports every family in bounded pages under one watermark, preserving JSON types and absolute expiries' / 'OPS-02: a watermark change between pages is detected (mutation sequence or maintenance version)' / 'OPS-02/ST-10: an import is bound to one backup file; a second backup of the same source cannot be mixed in'<br>tests/unit/backupFormat.test.ts > 'OPS-02: rejects a missing trailer, missing manifest and a missing chunk' / 'OPS-02: the family list is closed and carries no secret, session or credential columns'<br>tests/setup-cloudflare/operator.test.mjs > 'OPS-02 an interrupted export leaves no manifest or trailer and reports the held state' / 'OPS-02 import is resumable by chunk identity after an interruption' | The CLI is tested against a fake server, not the built artifact. Staging export and import rehearsals are remote. |
| OPS-03 | remote-gate | PITR restore with a new external epoch deployed before restore, controlled activation, conservative reconciliation; release classification and N−1→N→N−1 rollback rehearsal. | tests/workers/operator.test.ts (restore refusals and call sequence)<br>tests/unit/api.operator.routes.test.ts<br>tests/setup-cloudflare/operator.test.mjs<br>tests/workers/operator.test.ts > 'JOB-10: a restored alarm and open state stay inert under the epoch mismatch' / 'JOB-10/OPS-03: activation reconciles every nonterminal generation to recovery-required, then old envelopes, claims and results are rejected'<br>(+2 more) | A guarded restore entry point now exists (restoreToBookmark RPC, POST /api/operator/recovery/restore, operator:cloudflare recovery restore); local workerd refuses PITR, so refusals and the call sequence are tested with stubs. The PITR rehearsal is remote. |
| OPS-04 | remote-gate | Production transition runbook: target selection, metadata inventory, clean-start or preserve decision, staging rehearsal, drain, fence, prepare, switch, observe, retire; prepared clean-start and preserve-data runbooks. | docs/operations/cloudflare-migration/TRANSITION.md<br>tests/integration/inventoryRedis.test.ts (npm run test:inventory:redis)<br>RUNBOOK.md lines 153–166 (nine-step list) | TRANSITION.md provides target selection, the authorized metadata-only inventory (npm run inventory:redis, tested on disposable Redis), clean-start and preserve-data paths, the old-writer barrier and switch/observe/retire. Execution needs production authorization; an Upstash importer is built only if the inventory shows data to keep (package README). |
| VERIFY-01 | met-local | Local environment with real boundaries: vitest-plugin, the production artifact via createTestHarness, per-runner temp directories, rejection of inherited credentials and unexpected egress, and a fault manifest (cut, evidence, reply, next action) with restart tests. | tests/workers/faultManifest.ts + tests/unit/cloudflareFaultManifest.test.ts<br>tests/cloudflare-restart/*.restart.test.ts (npm run test:cloudflare:restart)<br>tests/cloudflare-artifact/hygiene.artifact.test.ts<br>tests/setup-cloudflare/check-lanes.test.mjs<br>(+4 more) | Cloudflare fault manifest (47 cuts, unit-checked), production-object process-kill restart lane, credential scrubbing in every harness and release lane, workers config pinned to production flags. |
| VERIFY-02 | met-local | Supported release matrix: existing quality, builds, browser and Redis lanes plus Cloudflare config, artifact, shared storage, jobs/API, browser and operational tooling, with one complete run on the final revision. | dist/cloudflare/artifact/receipt.json: status passed, source e42be63 (HEAD), checkedAt 2026-09-23T16:08:48Z, 17 lanes exit 0 (check, test:setup, installer, audit, diff-check, workers-runtime, import-boundary, redis-contract, redis-crash, adversarial, 3 Node builds, node-browser, cloudflare-artifact, cloudflare-restart, cloudflare-browser)<br>node scripts/cloudflare/check.mjs --list (lane inventory) | The receipt is a local git-ignored file from the concurrent release check, not committed evidence. MEASUREMENTS.md records earlier runs against dirty trees. Operational tooling is covered by the installer lane with a fake wrangler. |
| VERIFY-03 | remote-gate | End-to-end acceptance scenarios (study lifecycle, participant completion, tabs/preview, failed/ambiguous analysis, concurrent retries, config races, storage uncertainty, boundaries, export/follow-up, fixtures, restart/expiry) asserting storage and provider calls. | tests/e2e-cloudflare/analysis-controls.spec.ts<br>tests/e2e-cloudflare/demo.spec.ts<br>tests/e2e-cloudflare/research-workflow.spec.ts > 'participant saves before analysis runs; background analysis completes after the tab closes; researcher recovers a failure once, exports and aggregates'<br>tests/e2e-cloudflare/research-workflow.spec.ts > 'researcher preview never creates research records or jobs'<br>(+2 more) | Every case is covered locally (browser, artifact, restart, workers or integration tier), including the concurrent-retry race and the keyless demo in the Cloudflare browser lane. The Boundaries row (real memory and CPU for the 500-record export and 1,000-record reads) needs a deployed Worker. |
| VERIFY-04 | remote-gate | Remote staging and installation gates: canonical-origin cookies, Queue dispatch and consumer, alarm after inactivity, protected logs, schema init, restore, clean renamed install, repeat install/update, interrupted setup, deliberately failing release check, live provider smoke. | tests/setup-cloudflare/install.test.mjs > 'setup:cloudflare resume never adopts unrelated resources' (local fake-account precursors) | Nothing has run against a real account. Rehearsal items are spread across INSTALLER and RUNBOOK; there is no staging rehearsal script or checklist document. |
| VERIFY-05 | met-local | Local RC complete when the matrix passes on the final revision, surfaces are integrated, tooling is exercised, and a review packet maps requirements to evidence; remote gates stay pending. | docs/operations/cloudflare-migration/evidence/REVIEW-PACKET.md<br>dist/cloudflare/artifact/receipt.json (e42be63, passed, 17 lanes)<br>MEASUREMENTS.md §5 evidence index (fault cuts, provider request counts) | This packet maps all 55 IDs; the final-revision release matrix result is recorded below. |
